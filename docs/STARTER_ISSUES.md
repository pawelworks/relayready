# Starter issues

These are proposals for the owner to open as public issues after the repository location is confirmed.

1. Add a German relative-time word list with positive and negative tests.
2. Add a French relative-time word list with positive and negative tests.
3. Add a Spanish relative-time word list with positive and negative tests.
4. Add a Portuguese relative-time word list with positive and negative tests.
5. Add a Polish relative-time word list with positive and negative tests.
6. Extend secret detection with documented GitHub token patterns and false-positive tests.
7. Extend secret detection with documented cloud-provider key patterns and false-positive tests.
8. Add a code fixture whose dead end is a previously rejected dependency.
9. Add a code fixture whose invariant protects a migration script.
10. Add a knowledge fixture that reconciles two changed CSV extracts.
11. Add a knowledge fixture that refreshes a Markdown research note from local sources.
12. Verify and add a disabled runner entry for a new Codex CLI version.
13. Verify and add a disabled runner entry for a new Claude Code version.
14. Verify and add a disabled runner entry for goose.
15. Verify and add a disabled runner entry for Kimi CLI.
16. Prototype an importer for `chentaymane/handOff` output.
17. Prototype an importer for `agenthandoff` bundles.
18. Prototype an importer for AAHP directories.
19. Improve pointer-hash diagnostics for Windows paths.
20. Add property tests for Unicode invariant normalization.

