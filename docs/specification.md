# Specification

The normative specification is maintained at `spec/SPEC.md` in the repository root. The documentation build intentionally keeps that source authoritative rather than maintaining a second copy.

Browse that file in the source repository to read the complete version 0.1 contract.

