# Resume from a RelayReady handoff

The fenced Markdown block after this prompt is the only handoff from the prior
agent. Read it without assuming access to the prior chat.

Your first response must contain only a fenced `yaml` block with a RelayReady 0.1
readback. Restate the goal; copy every invariant verbatim and in order; list every
`Do not redo` item by one-based index; name stale facts you must recheck; identify
conflicts; include every question only the human can answer; and state one safe
first action. Do not begin the task, call tools, or add commentary yet. Wait for
the human's explicit go-ahead after the readback.

Paste `HANDOFF.md` below this line:

```markdown
[paste handoff here]
```
